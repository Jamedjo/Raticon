<Window x:Class="Raticon.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:rc="clr-namespace:Raticon.Control"
        DataContext="{Binding Main, Source={StaticResource Locator}}"
        Title="Raticon v1.0.0" Height="430" Width="526">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="30"></RowDefinition>
            <RowDefinition Height="*"></RowDefinition>
        </Grid.RowDefinitions>

        <ToolBar Grid.Row="0">
            <ToolBar.Resources>
                <Style TargetType="{x:Type ToolBarPanel}">
                    <Setter Property="Orientation" Value="Vertical"/>
                </Style>
            </ToolBar.Resources>
            <DockPanel>
                <Grid DockPanel.Dock="Right">
                    <Button Command="{Binding ClearThumbCacheCommand}">Clear Windows Thumbnail Cache</Button>
                </Grid>
                <StackPanel  DockPanel.Dock="Left" Orientation="Horizontal">
                    <Button Command="{Binding AddFolderCommand}">Select Folder</Button>
                    <Button Command="{Binding MakeIconsCommand}">Make Icons</Button>
                </StackPanel>
            </DockPanel>
        </ToolBar>

        <rc:FilmListView Grid.Row="1"/>
    </Grid>
</Window>
